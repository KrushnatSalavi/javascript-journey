const user = {
    name: "Krushna"
};

console.log(
    Object.getPrototypeOf(user)
);

const arr = [1, 2, 3];

console.log(
    Object.getPrototypeOf(arr)
);

// String Prtotype

const name = "Krushna";

console.log(name.toUpperCase());
